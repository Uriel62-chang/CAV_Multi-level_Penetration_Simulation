# 实验设计：v0.4.2 重设计（基本图方案 U55）

> 状态：**配置已落地（2026-08，待正式重跑授权）**。经 P0 插入缺陷终审
> （departSpeed="max" 致高密度插入损失）+ 交通流理论评估后，对 A 方案实验参数做
> 重设计。设计口径已由用户确认；warmup 已由 9 档 SUMO 标定闭环（§四）；
> `configs/v0.4.2/main.json` 已按本设计落地（7,524 runs，dry-run 通过）。
> 正式重跑前需多轮审核（时间成本高）。
>
> 关联：`docs/development/v0.4.2-grid-design.md`（旧 A 方案冻结记录）、
> `docs/development/v0.4.2-split-design.md`（合并设计）、CHANGELOG。

---

## 一、背景与动机

1. **P0 插入缺陷（已修复）**：`flow_generator` 原以 `departSpeed="max"` 插入，
   高密度档（饱和环路无 ~123 m 制动空档）大量车辆永久滞留插入队列、从不进仿真，
   且完全静默（`vehicle_type_map` 记录全 vehN、解析只遍历已离场车辆）。已实测
   确认修复前损失（12 档，cav=0.5）：s1 v120 仅插入 62/120、s2 v240 132/240、
   s3 v240 221/240 等。**修复**：`departSpeed="0"`（静止插入，warmup 吸收冷启动）。
2. **密度对齐失真**：A 方案"每道 5–60 veh/km/道"标签在高密度档未实现（缩减车队），
   且 60 veh/km/道本身即拥堵区（HV 平衡速度仅 22 km/h），`warmup=600` 不足以保证
   稳定车流形成（60 veh/km/道 HV ≈ 760 s）。
3. **实验目标升级**：从"限定可观测流量范围内指标随渗透率变化"升级为
   **实现通行能力基本图（fundamental diagram）**——观测窗覆盖自由流 → 临界 →
   拥堵 → 近阻塞的完整密度–流量关系，呈现山峰状右偏基本图，具备交通工程价值。

## 二、设计口径（用户确认，2026-08）

1. **密度上限取各场景近阻塞密度（非完全阻塞）**：旨在体现交通流基本图形态
   （山峰 + 右偏长尾），不测完全静滞段。
2. **`departSpeed="0"`**：确保 warmup 前形成稳定车流；闭环路网照应现实高等级
   路段（高速公路、城市快速路）。
3. **warmup 时长联立 ② 确定**：由 SUMO 实测标定（见 §四），不按公式拍脑袋。

## 三、密度轴设计（U55：上限 55 veh/km/道，内存约束修订）

- **阻塞密度**（间距=车长+minGap）：HV 133 veh/km/道（7.5 m）、CAV(CACC) 182
  veh/km/道（5.5 m）。
- **自由流临界密度**（v=33.33 稳态）：HV 17.4、CAV(CACC) 39.2 veh/km/道；
  通行能力 q_max(HV) ≈ 1/τ = 2,400 veh/h/道。
- **密度轴**（环路 2000 m；密度 = vehN / (车道数 × 2 km)）：

| 场景 | 车道 | vehN 轴 | 档数 | 密度范围 |
|---|---|---|---|---|
| s0 / s1 | 1 | 10,20,…,110（11 档） | 11 | 5–55 veh/km/道 |
| s2 / s3 | 2 | 20,40,…,220（11 档） | 11 | 5–55 veh/km/道 |

- **临界区加密**：HV 峰值在 kc≈17.4、CAV(CACC) 在 39.2，随 cav_count 11 档连续
  移动。vehN 必须为 10 的倍数（维持 0.1 步长 cav_counts 全整数），故加密点取
  vehN 10 倍数档：s0/s1（单车道，密度=vehN/2）10–25 veh/km/道对应 vehN
  20/30/40/50（密度 10/15/20/25）；s2/s3（双车道，密度=vehN/4）对应 vehN
  60/80/100。**U55 轴步长 10（s0/s1）/20（s2/s3）本身即密度步长 5 veh/km/道，
  已覆盖 HV 临界（15/20）与 CAV 临界（35/40）邻域，无需额外加密档**。
- **每 treatment 展开**：cav_count 0.1 步长 11 档（全整数）、端点 assignment 失活
  sentinel、SUMO seed 保留——与 A 方案 `_build_cav_count_specs` 展开规则一致。
- **run 数**：4 场景 × 11 档 × 171 runs/档 = **7,524 runs**。

### s3 瓶颈语义（单独口径）

s3 瓶颈段（e15/e16，125 m 单车道）阻塞容量仅 ~17 辆、通行能力 2,400 veh/h
（τ 限制）。s3 在 U55 轴（主线 220 辆）瓶颈深度过饱和——s3 的"基本图"是
**瓶颈排队–吞吐关系**而非主线基本图。决策：**s3 与 s0/s1/s2 同密度轴**
（保持跨场景密度对齐的原有设计意图），报告时 s3 单独成图并标注瓶颈语义。

## 四、warmup 标定（已定稿 2026-08）

- **方法**：`departSpeed="0"` 下跑 9 档（覆盖密度谱关键点 + 车型混合 + 瓶颈），
  FCD 1s 全程记录，逐 timestep 平均速度找收敛点。
- **结果（全部 ≤120 s 达到稳态速度，采样粒度 120 s）**：

| 档 | 密度 / 组成 | 稳态平均速度 | 达到稳态 |
|---|---|---|---|
| s0 v20 | 10 veh/km/道，HV 自由流 | 16.1 m/s（波动 14–18） | ≤120 s |
| s2 v160 | 40，HV 中间密度 | 10.3 m/s（极稳） | ≤120 s |
| s2 v240 | 60，HV | 5.2 m/s | ≤120 s |
| s2 v240 c0.5 | 60，HV/CAV 混合 | 8.9 m/s | ≤120 s |
| s2 v360 | 90，HV | 1.9 m/s | ≤120 s |
| s3 v240 | 60，HV 瓶颈 | 2.7 m/s（波动 1.6–3.0） | ≤120 s |
| s3 v360 | 90，HV 瓶颈 | 1.6 m/s（波动） | ≤120 s |
| s0/s1 v180 | 90，HV | 1.9 m/s | ≤120 s |

- **定稿参数**：**warmup=600 s**（9 档最大稳定形成 ≤120 s，余量 5 倍）、
  **simulation_end=1800 s**（观测窗 [600,1800) = 1200 s，圈时完整性边界 ≤67 veh/km/道）。"中间密度更慢"假设被
  实测否定（s2 v160 最稳）；CAV 混合（τ 更小）稳定不慢于纯 HV。
- **附加发现（报告边界实锤）**：自由流档稳态平均速度显著低于 120 km/h——
  s0 v20 为 16.1 m/s（58 km/h），**主因是 s0 几何转角限速**（方形 90° 转角，
  net 内部车道 speed=3.90 由 limitTurnSpeed 生成；free-flow 单辆 111.8 s/圈
  ≈17.9 m/s 实证），自发车队效应（Krauss σ 扰动 → 车辆聚合）为次要叠加
  （20 辆 16.1 vs 单辆 17.9 m/s）；s1/s2/s3（32 边形平滑几何）无转角限速，
  其自由流分支压低才归因于车队效应。s0 的"转角受限基线"是 s0→s1 geometry
  smoothing 对照轴的设计内特征（非缺陷），报告按设计内差异声明。

## 五、指标口径

- **流量/密度用实测**：detector 实测流量 + 实测密度（占据率换算或实际车辆数/环长）
  为权威；名义 vehN 仅作设计标签。
- **双口径并存记录**：全局密度（vehN/环长）与局部密度（检测器/排队段）分别记录，
  报告时声明所用口径。
- **密度/FD 交付链声明（收敛审核 P2）**：schema/CSV 当前无密度或占据率列、可视化
  无 FD 图——全局密度可由 vehN/场景车道数解析推导，FD 原始数据（mean_flow/
  mean_speed 聚合列）已齐备，属**报告链补充项**（重跑后由报告补充 FD 图与密度
  口径声明），不影响结果正确性。
- 效率/安全/环保指标沿用 schema=2 全链路（writer/aggregate/subgroup 不变）。

## 六、仿真成本与资源

- run 数 7,524（vs A 方案 8,208 −8%）；最慢档（s3 v220）wall 预估 ~200–300 s。
- **3 workers 定案（2026-08，内存约束下固定并发最优）**：总 wall 预估 **~24 h**
  （7,524 runs × 观测窗 1200 s；事件驱动模拟：3w 失败 5.6%/24h vs 6w 失败 50%/32h——
  s2 高内存段 2–3 个并发即满，workers≥4 成批 OOM 吞噬并发收益；WSL2 24 GB）。
- **密度上限修订（内存约束，2026-08 实测）**：s2 全 CAV 峰值 v160=9.51 GiB、
  **v200=22.69 GiB / v220=22.67 GiB（均 SUCCESS，贴 24 GB+swap 边缘）**、v230+ >22.7 GiB（OOM）——
  SSM 设备跟踪开销随密度增长（P1-3 探针证实 s2 零事件：sim 内存高 ≠ 事件量，
  是 SUMO SSM 设备对车辆对的跟踪开销）。故统一密度上限 55 veh/km/道（v220 双车道/v110 单车道；
  v100 单车道；s0/s1/s2/s3 一致，保持递进对比的密度对齐），基本图右端从
  原 U90 的 68% kj 收缩至 37.5% kj（仍覆盖临界 17.4 与拥堵区中段；“近阻塞”语义
  改为“拥堵区上探受限”，报告声明）。v220 档单进程可跑通（resume 降并发补
  跑），3 workers 首轮并发失败 run 全部可补。
- OOM 走降并发 resume（不变）；SSM 全开（合并设计不变）。

## 七、报告边界（须声明）

1. **几何转角限速（s0 专属，设计内特征）**：s0 方形 90° 转角（net 内部车道
   3.9 m/s）把自由流速度钉在 ~17.9 m/s（单辆圈时 111.8 s），通行能力受转角
   吞吐（~1400–1900 veh/h）限制而非 2400，FD 峰位落在 k≈22–30（非 kc≈17.4）。
   这是 s0→s1 geometry smoothing 对照轴的自变量（设计内差异，非缺陷）；
   s0 的排放/TTC/时损受转角反复加减速放大——报告按场景独立声明基线。
2. **闭环有限环长效应**：2000 m 环上密度波动无法消散，低密度自由流档
   （5–15 veh/km/道）可能出现自发拥堵（phantom jam）压低自由流分支实测流量
   （s1/s2/s3 无转角限速，其自由流压低主要归因于此；s0 以转角限速为主因）。
2. **冷启动等价性**：departSpeed=0 全车同时注入 vs 现实渐进注入的稳态等价性，
   建议 1–2 档对照验证（可选）。
3. **s3 瓶颈语义**：s3 基本图为瓶颈排队–吞吐关系，不与 s0/s1/s2 的主线基本图
   直接比较。
4. **基本图形态**：数据支撑的基本图为右偏山峰状，山峰顶点（通行能力）受
   SUMO 参数（τ、decel）与车队异质性（HV/CAV 混合）影响，报告标注理论值对照。
5. **低密度自发拥堵 + 自由流分支低估（审查实证，范围扩展）**：s0 v10 实测
   stop-and-go（车速 3.5–29 m/s 并存，k=5 flow 仅 ~300 veh/h）；审查进一步实测
   **s1（平滑几何、无转角限速）同样显著低估**：k=10 flow=840（较理论 k·v_f=1170
   低 28%）、峰值 k=20=1695（仅理论 2400 的 71%）、k=30–40 吞吐倒挂
   （1695→1635→1461）——低估是闭环普遍现象（自发车队/有限环长），非 s0 专属。
   报告须对**全部场景**声明自由流分支与中间密度形态边界（而非仅 s0）。
6. **圈计数选择偏差（审查 P1-2，s3 最薄）**：vehroute 圈只计入 lap_start ∈
   [600, 1800−T) 的完整圈；s3 最慢档（v220 圈时 ≈700 s）仅 ~74% 车辆窗口内
   开始圈次，且与"圈开始时是否卡在瓶颈队尾"相关（非均匀相位采样）→
   mean/p95_lap_delay_s 有系统偏差；若任一圈时 >1200 s → completed_lap_count=0
   → _all_lap_stats_missing 整 run（含全部 seed）剔除。**重跑监控项**：解析后
   检查 s3 高密度档 completed_lap_count>0，整组剔除时降级报告（不得静默丢失）。
7. **THW 为条件样本（审查 P2-1）**：FCD 无 leader 的样本（实测 s0 v10 约 19%，
   恰为最大间距样本）被排除出 THW——mean_thw_s 系统低估、thw_lt_1s_ratio 分母
   偏小；报告须声明 THW 为"有 leader 条件样本"。
8. **SUMO 积分方式（审查 P2-3）**：HV actionStepLength=1.0 触发 SUMO 自动
   step-method.ballistic（每 run stderr 告警），全局积分方式被静默改变；参考
   基准同条件测得（相对一致），报告声明。
9. **速度口径（审查 P2-6/P2-7，图层面执行）**：detector mean_speed 为算术平均
   （非 harmonicMeanSpeed，行程时间口径应谐波平均）且只取非零流量窗口——FD 主图
   只用 flow 影响有限，但**若报告补 v–k 图，拥堵分支速度会被系统性高估**，须改用
   谐波平均口径展示或标注偏差方向；detector_speed_window_count 实为"非零流量
   窗口数"（零流量窗被排除）——列名与语义差异在报告注明。

## 八、待办

1. [x] warmup 标定实测（本 § 四，9 档闭环，warmup=600 定稿）
2. [x] 配置落地：`configs/v0.4.2/main.json` treatments（U55 轴 7,524 runs +
      临界区由轴本身覆盖 + warmup=600/simulation_end=1800）
3. [x] 测试断言更新（planned_run_count=7,524 / endpoint=396 / dry-run）
4. [x] 文档同步（grid-design / split-design / handover / README / CHANGELOG /
      AGENTS / release-checklist / DEVELOPMENT）
5. [ ] 正式重跑 7,524 runs（红线，另行授权；重跑前多轮审核）
6. [ ] 重跑后按 writer/aggregate/handover/inventory 链路重建结果

## 九、决策记录

| 日期 | 决策 |
|---|---|
| 2026-08 | 口径 ①近阻塞密度（非完全阻塞）②departSpeed=0 ③warmup 实测标定——用户确认 |
| 2026-08 | 方案选 U90（68% kj）非 U120（90% kj）：右端无新增信息、成本翻倍 |
| 2026-08 | s3 与主场景同轴，报告单独成图并标注瓶颈语义 |
| 2026-08 | warmup=600 定稿（9 档标定 ≤120 s 稳定）；simulation_end 后经 U90/U55 重设计改 1800（观测窗 [600,1800)） |
| 2026-08 | `main.json` 落地 U55（7,524 runs，config_version=v0.4.2-main-7-U55-v220cap） |
